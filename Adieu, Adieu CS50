import inflect
p = inflect.engine()
reading = True
names = []
while reading:
    try:
        user_input = input("Name: ")
        names.append(user_input)
        
    except EOFError:
        reading = False
        namez = p.join(names)
        print(f"\nAdieu, adieu, to {namez}")
