import re
import sys


def main():
    print(parse(input("HTML: ")))


def parse(s):
    link = re.search(r'^[\w\s0-9\d\\<>"/=;]*src=(?:"https://|"http://)((www\.)?youtube\.com/embed/\w+)"[\w\s0-9\d\\<>/=;]*', s)
    if link:
        url = link.group(1)
        return url

...


if __name__ == "__main__":
    main()
